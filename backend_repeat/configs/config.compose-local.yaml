# 中间件跑在 Docker (vid_redis)，后端服务跑在主机
# 启动方式: docker compose -f docker-compose.local.yml up -d

server:
  port: 8080

database:
  host: localhost
  port: 3307
  user: root
  password: "123456"
  dbname: feedsystem

redis:
  host: localhost
  port: 6380
  password: "123456"
  db: 0

rabbitmq:
  host: localhost
  port: 5672
  username: admin
  password: password123

observability:
  pprof:
    enabled: true
    api_addr: localhost:6062
    worker_addr: localhost:6061
